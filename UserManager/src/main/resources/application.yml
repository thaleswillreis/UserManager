# Spring Boot configuration
spring:
  profiles:
    active: dev
  # Database
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/authdb
    username: postgres
    password: 1234567
  # JPA properties
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
    generate-ddl: true
# Server configuration
server:
  port: 8080

security:
  jwt:
    token:
      secret: SqkW"pzLpD4KGg_Q3*5E(KcDH)*@g"r~C~dA!^Y,C+:m[^4,EcN%nVAE@(<UaATw
      expiration: 300000 #86400000 (5 minutos)